<Window x:Class="LANChatAdmin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="系统控制台" Height="600" Width="900"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown">

    <Border Background="#F7F4F2" CornerRadius="28" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Color="#000000" Opacity="0.12" ShadowDepth="2"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#ECE6E2" CornerRadius="28,0,0,28">
                <Grid Margin="24,32,24,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Border Background="#0061A4" CornerRadius="8" Width="45" Height="24">
                                <TextBlock Text="聊天室" Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Width="34"/>
                            </Border>
                            <TextBlock Text="控制台" FontSize="16" FontWeight="Black" Foreground="#1A1C1E" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <Border Background="#FFFFFFFF" CornerRadius="16" Padding="16">
                            <StackPanel>
                                <TextBlock Text="服务状态" FontSize="11" Foreground="#73777F" FontWeight="Bold"/>
                                <TextBlock x:Name="TxtServerStatus" Text="正在探测后端..." FontSize="14" FontWeight="Bold" Foreground="#0061A4" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Button Grid.Row="2" Content="挂起至系统托盘" Background="#0061A4" Foreground="White" FontWeight="Bold" FontSize="12" Height="40" BorderThickness="0" Click="BtnMinimizeToTray_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-16,-16,0">
                    <Button Content="—" Background="Transparent" Foreground="#43474E" Width="40" Height="40" BorderThickness="0" FontSize="14" Click="BtnMinimize_Click"/>
                    <Button Content="✕" Background="Transparent" Foreground="#43474E" Width="40" Height="40" BorderThickness="0" FontSize="14" FontWeight="Bold" Click="BtnClose_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="0" Text="快捷功能矩阵" FontSize="20" FontWeight="Black" Foreground="#1A1C1E" Margin="0,0,0,24"/>

                <UniformGrid Grid.Row="1" Columns="2" Rows="1" Margin="0,0,0,24">
                    <Border Background="White" CornerRadius="24" Padding="20" Margin="0,0,8,0" BorderBrush="#D5D8DF" BorderThickness="1">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                                <TextBlock Text="全场禁言" FontSize="14" FontWeight="Bold" Foreground="#1A1C1E"/>
                                <TextBlock Text="非公告信息不得发送" FontSize="11" Foreground="#73777F" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Border x:Name="BorderToggleMute" DockPanel.Dock="Right" CornerRadius="12" Padding="4" Background="Transparent" BorderBrush="#D5D8DF" BorderThickness="1" Width="70">
                                <CheckBox x:Name="ToggleMute" Style="{StaticResource {x:Static ToolBar.CheckBoxStyleKey}}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="44" Height="24" Checked="ToggleMute_Checked" Unchecked="ToggleMute_Unchecked" Background="#FF15FF00"/>
                            </Border>
                        </DockPanel>
                    </Border>

                    <Border Background="White" CornerRadius="24" Padding="20" Margin="8,0,0,0" BorderBrush="#D5D8DF" BorderThickness="1">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                                <TextBlock Text="系统开机自启" FontSize="14" FontWeight="Bold" Foreground="#1A1C1E"/>
                                <TextBlock Text="自启动" FontSize="11" Foreground="#73777F" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Border x:Name="BorderToggleAutostart" DockPanel.Dock="Right" CornerRadius="12" Padding="4" Background="Transparent" BorderBrush="#D5D8DF" BorderThickness="1" Width="70">
                                <CheckBox x:Name="ToggleAutostart" Style="{StaticResource {x:Static ToolBar.CheckBoxStyleKey}}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="44" Height="24" Checked="ToggleAutostart_Checked" Unchecked="ToggleAutostart_Unchecked" Background="#FF15FF00"/>
                            </Border>
                        </DockPanel>
                    </Border>
                </UniformGrid>

                <Border Grid.Row="2" Background="White" CornerRadius="24" Padding="24" BorderBrush="#D5D8DF" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="📢 高危系统广播定向群发" FontSize="12" FontWeight="Bold" Foreground="#0061A4" Margin="0,0,0,12"/>
                        <TextBox x:Name="TxtBroadcast" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" Background="#F7F4F2" BorderThickness="0" Padding="16" FontSize="12" Foreground="#1A1C1E" VerticalAlignment="Stretch" VerticalContentAlignment="Top" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" GotFocus="TxtBroadcast_GotFocus" LostFocus="TxtBroadcast_LostFocus"/>
                        <Button Grid.Row="2" Content="向全部终端群发信号" Background="#0061A4" Foreground="White" FontWeight="Bold" FontSize="12" Height="44" BorderThickness="0" Margin="0,16,0,0" Click="BtnBroadcast_Click"/>
                    </Grid>
                </Border>
                <!-- 简洁提示条 -->
                <Border x:Name="NotifyPanel" Grid.Row="2" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,12,0,0" Background="#002F5A" CornerRadius="8" Padding="12" Width="320">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <TextBlock x:Name="NotifyText" Foreground="White" FontSize="13" VerticalAlignment="Center" Text="" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>